name: spec_command
description: "Manage tasks/*.yaml specs via /spec list/show/approve/reject"
version: "0.1.0"
type: inline

llm_tier: worker

triggers:
  - "/spec"

cost_estimate: low
approval_policy: auto

side_effects:
  - reads_filesystem
  - writes_filesystem
  - calls_llm
  - sends_telegram_message

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  leaf_modules:
    - src.skills.spec_command.parser
  skills: []

source: manual
created_at: "2026-04-26"

tests:
  contract: tests/skills/spec_command/test_contract.py
  behaviour: tests/skills/spec_command/test_skill.py
