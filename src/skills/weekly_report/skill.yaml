name: weekly_report
description: "Builds weekly report across priority pillars"
version: "0.1.0"
type: inline

llm_tier: worker

triggers:
  - "/weekly_report"

cost_estimate: low
approval_policy: auto

side_effects:
  - reads_filesystem

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  modules:
    - src.news
    - src.archive
    - src.pillars

source: manual
created_at: "2026-05-07"

tests:
  contract: tests/skills/weekly_report/test_skill.py
  behaviour: tests/skills/weekly_report/test_formatter.py
