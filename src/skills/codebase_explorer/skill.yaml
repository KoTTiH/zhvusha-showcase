name: codebase_explorer
description: "Read-only анализ кодовой базы из обычного чата"
version: "0.1.0"
type: inline

llm_tier: worker

triggers: []

cost_estimate: medium
approval_policy: auto

side_effects:
  - reads_workspace
  - reads_filesystem
  - calls_llm
  - sends_telegram_message
  - delegates_to_code_agent

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  leaf_modules:
    - src.skills.codebase_explorer.skill
    - src.agent_runtime
  skills:
    - code_agent

source: manual
created_at: "2026-05-08"

tests:
  behaviour: tests/skills/codebase_explorer/test_skill.py
