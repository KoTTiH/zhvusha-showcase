name: cycle_analyzer
description: "Lookup archived self-coding cycle insights"
version: "0.1.0"
type: inline

llm_tier: worker

triggers:
  - "/archive_lookup"

cost_estimate: low
approval_policy: auto

side_effects:
  - reads_filesystem

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  modules:
    - src.archive

source: manual
created_at: "2026-05-07"

tests:
  contract: tests/skills/cycle_analyzer/test_skill.py
  behaviour: tests/skills/cycle_analyzer/test_analyzer.py
