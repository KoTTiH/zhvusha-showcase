name: digital_scenario
description: "Natural-language router for digital-agent scenarios"
version: "0.1.0"
type: inline

llm_tier: worker

triggers: []

cost_estimate: low
approval_policy: auto

side_effects:
  - calls_llm

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  leaf_modules:
    - src.skills.digital_scenario.skill
    - src.agent_runtime.digital_scenarios
    - src.agent_runtime.digital_scenario_coverage

source: manual
status: experimental

tests:
  behaviour: tests/skills/digital_scenario/test_skill.py
