name: topic_to_spec
description: "Преобразует topic_clusters в candidate spec/proposal с источниками"
version: "0.1.0"
type: inline

llm_tier: worker

triggers:
  - "/topic_to_spec"

cost_estimate: low
approval_policy: auto

side_effects:
  - reads_filesystem
  - writes_filesystem

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  modules:
    - src.news
    - src.pillars
    - src.skills.spec_command.parser
    - src.skills.proposal_command

source: manual
created_at: "2026-05-07"

tests:
  contract: tests/skills/topic_to_spec/test_skill.py
  behaviour: tests/skills/topic_to_spec/test_builder.py
