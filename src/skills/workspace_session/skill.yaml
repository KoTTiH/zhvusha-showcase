name: workspace_session
description: "Runs Zhvusha's morning reflection session"
version: "1.0.0"
type: inline

llm_tier: strategist

triggers:
  - "/morning"

cost_estimate: high
approval_policy: auto

side_effects:
  - calls_llm
  - calls_llm_tier_strategist
  - spawns_subprocess
  - reads_workspace
  - writes_workspace
  - writes_to_kb
  - sends_telegram_message
  - modifies_memory

mode_tags:
  - personal

modifies: []

parameters: {}  # tunables live in src.core.config.Settings (workspace_path, morning_session_model, codex_cli_path)

dependencies:
  leaf_modules: []
  skills: []

source: manual
created_at: "2026-04-18"

tests:
  contract: tests/skills/workspace_session/test_contract.py
  integration: tests/skills/workspace_session/test_integration.py
