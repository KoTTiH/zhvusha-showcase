name: post_drafts
description: "Creates channel post drafts from topic backlog"
version: "0.1.0"
type: inline

llm_tier: worker

triggers:
  - "/post_drafts"

cost_estimate: medium
approval_policy: required

side_effects:
  - reads_filesystem
  - writes_workspace

mode_tags:
  - personal

modifies: []

parameters: {}

dependencies:
  modules:
    - src.news

source: manual
created_at: "2026-05-07"

tests:
  contract: tests/skills/post_drafts/test_skill.py
  behaviour: tests/skills/post_drafts/test_models.py
